import React from 'react';

import * as Icons from '../../../react';

export default {
  args: {
    style: {
      height: 24,
      width: 24,
    }
  },
  title: 'Icons'
};

export const ArrowBottom = (props) => <Icons.ArrowBottom {...props} />

export const ArrowLeft = (props) => <Icons.ArrowLeft {...props} />

export const ArrowRight = (props) => <Icons.ArrowRight {...props} />

export const ArrowTop = (props) => <Icons.ArrowTop {...props} />
